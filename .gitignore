# Code generation
#/gen -- commented out to avoid having the reviewer generate the code / install further deps on their machine
#/ent/* -- commented out to avoid having the reviewer generate the code / install further deps on their machine
!/ent/schema
!/ent/migrations

# Executables
/cmd/migrate/migrate
/cmd/seed/seed
/cmd/client/client
/server/server
